{
  "name": "@aigentos/core",
  "version": "0.1.0",
  "description": "AigentOS Core Runtime — the portable kernel that installs, manages permissions for, and executes AI agents. Runs as a standalone local service so any shell (desktop app today, a Linux distro tomorrow) can talk to it over HTTP/WebSocket.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "uuid": "^9.0.1",
    "ws": "^8.17.0"
  }
}
